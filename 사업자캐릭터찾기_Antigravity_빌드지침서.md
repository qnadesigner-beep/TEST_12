# 사업자 캐릭터 찾기 — Antigravity 빌드 지침서

> Antigravity 워크스페이스에 그대로 복붙해서 쓰는 실행 문서.
> 앞서 만든 「캐릭터 매트릭스 & 아트 디렉션 지침서」가 이 문서의 입력값이다.

---

## 0. 먼저 확인 — 2026년 7월 기준 Antigravity 현황

2025년 11월 프리뷰 이후 구조가 한 번 크게 바뀌었다. 예전 방식으로 세팅하면 안 맞는다.

| 항목 | 현재 |
|---|---|
| 버전 | **2.0** (2026년 5월 19일, I/O 2026에서 공개) |
| 형태 | 데스크톱 앱 + **CLI**(Go) + **SDK**. 에디터 중심 → **멀티 에이전트 오케스트레이션 중심**으로 이동 |
| 기본 모델 | Gemini 3.5 Flash. Claude Sonnet 4.6 / Opus 4.6 / Gemini 3 Pro 선택 가능 |
| 주의 | Gemini CLI · Gemini Code Assist 확장은 **2026년 6월 18일 소비자 접근 종료** → Antigravity CLI로 이관됨 |
| 요금 | 개인 무료(레이트 제한). 상위는 AI Pro / AI Ultra($100/월) |

**핵심 변화 3개만 기억:**
1. 에이전트를 **여러 개 동시에** 굴린다 (서브에이전트 분할, 백그라운드 스케줄)
2. **브라우저 서브에이전트**가 실제 크롬에서 테스트하고 녹화를 남긴다 → 코딩 모르는 상태에서 검수 가능한 유일한 수단
3. 규칙은 `AGENTS.md` + `skills/` + `workflows/` 3층으로 관리한다

> 위 정보는 웹 검색으로 확인한 2026년 7월 시점 기준. 세부 UI·요금은 바뀔 수 있으니 첫 실행 시 Changelog 한 번 확인할 것.

---

## 1. 스택 판단

| 후보 | 판단 |
|---|---|
| **Next.js(App Router) + Supabase + Vercel** | **채택.** OG 이미지 자동 생성(`@vercel/og`)이 결과 카드 30종을 서버에서 그려준다. 이게 없으면 공유 카드를 수동으로 30장 만들어야 한다 |
| 순수 정적 SPA + localStorage | 기각. 마이크로 피드백의 실측 비율(`가장 많은 사장님이 여기 있습니다`)을 못 만든다. 공유율 지표도 못 잡는다 |
| 노코드(타입폼·왈라 등) | 기각. 30조합 결과 카드 분기와 엠블럼 합성이 안 된다 |

**DB는 딱 2테이블.** 오버엔지니어링이 이 프로젝트의 가장 큰 리스크다.

---

## 2. 워크스페이스 구조

```
biz-character-quiz/
├─ AGENTS.md                  ← 에이전트 팀 정의 (최상위 규칙)
├─ skills/
│  ├─ quiz-scoring/SKILL.md   ← 채점 로직. 절대 임의 변경 금지 영역
│  ├─ result-card/SKILL.md    ← 결과 카드 & OG 이미지
│  ├─ visual-system/SKILL.md  ← 컬러·타이포 토큰 (아트 지침서 → 코드)
│  └─ analytics/SKILL.md      ← 지표 3개만
├─ workflows/
│  ├─ startcycle.md           ← /startcycle
│  └─ verify.md               ← /verify (브라우저 서브에이전트 검수)
├─ content/
│  ├─ questions.ts            ← 12문항 (문구 수정은 여기서만)
│  └─ characters.ts           ← 6캐릭터 카피
└─ public/emblems/            ← 엠블럼 6 + 배지 5
```

**원칙: 문구와 로직을 물리적으로 분리한다.** 카피를 고칠 때마다 에이전트가 로직을 건드리면 채점이 조용히 망가진다. `content/`는 사람이 고치고, 나머지는 에이전트가 고친다.

---

## 3. AGENTS.md (전문 — 그대로 복붙)

```markdown
# 프로젝트: 사업자 캐릭터 찾기

## 제품 한 줄
오프라인 사장님이 12문항에 답하면 6개 사업자 캐릭터 중 하나를 받는 진단 웹앱.
목표는 정확한 진단이 아니라 **완주와 공유**다.

## 절대 규칙 (위반 시 작업 중단하고 보고할 것)
1. 결과 화면에 총점·등급·순위·별점·레벨을 절대 표시하지 않는다. 스탯은 막대만, 숫자 없음.
2. `content/questions.ts`의 문구를 에이전트가 임의로 수정하지 않는다. 수정이 필요하면 제안만 하고 멈춘다.
3. `skills/quiz-scoring`의 배점표를 임의로 바꾸지 않는다.
4. 사람 얼굴·신체 이미지를 생성하거나 사용하지 않는다.
5. 응답자 개인정보(이름·연락처·사업자번호)를 수집하지 않는다. 익명 세션 ID만 쓴다.
6. 새 라이브러리 추가는 1개당 이유 1줄과 함께 보고 후 진행한다. 기본은 추가하지 않는 것.

## 팀 구성

### PM 에이전트
- 요구사항을 Implementation Plan 아티팩트로 먼저 만든다. 계획 없이 코드를 쓰지 않는다.
- 범위가 늘어나면 잘라낸다. 이 제품의 MVP는 "12문항 → 결과 카드 → 공유"가 전부다.
- 관리자 페이지, 로그인, 결제, 다국어는 MVP에 포함하지 않는다.

### 프론트엔드 에이전트
- Next.js App Router + TypeScript + Tailwind.
- 모바일 우선. 기준 뷰포트 390×844. 데스크톱은 중앙 정렬 컨테이너 max-width 480px.
- 문항 화면에서 질문 아래 24px 안에 첫 선택지가 온다. 스크롤 없이 3개 선택지가 다 보여야 한다.
- 상단 진행 표시는 숫자(`1/12`)가 아니라 얇은 막대 + "약 1분".
- 애니메이션은 문항 전환 200ms 페이드 하나만. 그 외 금지.

### 백엔드 에이전트
- Supabase. 테이블 2개만.
  - `sessions(id, started_at, completed_at, result_char, result_weapon, result_path, referrer)`
  - `answers(session_id, q_index, choice_index, answered_at)`
- 서버 라우트에서만 쓰기. 클라이언트 직접 삽입 금지.
- RLS 켜고 익명 insert만 허용, select는 집계 뷰로만.

### QA 에이전트 (브라우저 서브에이전트)
- 실제 브라우저에서 12문항 완주 시나리오를 3회 돌린다: 전부 1번 / 전부 3번 / 무작위.
- 각 시나리오의 결과 캐릭터가 채점표와 일치하는지 확인한다.
- 390×844에서 각 문항 화면 녹화를 남긴다. 선택지가 접히면 실패로 보고한다.
- 공유 카드 OG 이미지를 실제로 fetch해서 렌더 결과를 캡처한다.

## 산출물 규칙
- 모든 작업은 Task List → Implementation Plan → 구현 → Walkthrough 순서.
- 완료 보고에는 반드시 브라우저 녹화 또는 스크린샷을 첨부한다.
  (이 프로젝트 소유자는 코드를 직접 읽지 않는다. 화면으로 검수한다.)
```

---

## 4. skills/quiz-scoring/SKILL.md (핵심 — 여기만 정확하면 나머지는 고쳐도 된다)

```markdown
---
name: quiz-scoring
description: 12문항 응답을 6개 캐릭터 + 무기 + 길로 변환하는 채점 규칙. 결과 산출 관련 작업 시 반드시 사용.
---

# 채점 규칙

## 입력
`answers: number[12]` — 각 원소는 해당 문항에서 고른 선택지의 **원본 인덱스**.
화면에서는 선택지를 셔플하지만, 저장은 반드시 원본 인덱스로 한다.

## 1단계 — 두 축 계산
- R (반복성) = Q1~Q4 원본 인덱스 합 (0~8)
- L (레버리지) = Q5~Q8 원본 인덱스 합 (0~8)

구간:
- R: 0–2 = flow / 3–5 = repeat / 6–8 = stack
- L: 0–3 = self / 4–8 = system

## 2단계 — 캐릭터 매핑
| R \ L | self | system |
|---|---|---|
| flow | jangin (장인 匠) | wonjeong (원정대장 遠) |
| repeat | teojut (터줏대감 址) | sangdan (상단주 商) |
| stack | myeongjang (전속명장 印) | yeongju (영주 領) |

## 3단계 — 무기 (Q9, Q10)
- 무기 = Q10 선택값. `hand / seal / banner / gear / circle`
- Q9 === Q10 → `weaponConfirmed = true` (배지에 액센트 외곽 링 추가)
- Q9 !== Q10 → 결과 카드에 한 줄 삽입:
  "지금까지는 {Q9라벨}로 버텼고, 앞으로는 {Q10라벨}에 걸고 있다."

## 4단계 — 길 (Q11) / 동행자 (Q12)
- 길 = Q11: `commission / market / fame / covenant / archetype`
- 동행자 = Q12: 0 미동행 / 1 조력자 / 2 분신 → "다음 한 수" 문장의 접두어를 바꾼다.

## 출력
```ts
{ character, weapon, weaponConfirmed, path, companion, R, L }
```

## 절대 금지
- R+L을 합산한 총점을 만들지 않는다.
- 캐릭터에 순서·등급·우열을 부여하지 않는다.
- 배점표를 임의로 조정하지 않는다. 변경이 필요하면 제안만 하고 멈춘다.

## 테스트 케이스 (반드시 통과)
- 전부 0번 선택 → R=0, L=0 → jangin
- 전부 2번 선택 → R=8, L=8 → yeongju
- Q1~4=2,2,2,2 / Q5~8=0,0,0,0 → R=8, L=0 → myeongjang
- Q1~4=0,0,0,0 / Q5~8=2,2,2,2 → R=0, L=8 → wonjeong
```

---

## 5. skills/result-card/SKILL.md (요약)

```markdown
---
name: result-card
description: 결과 화면과 공유용 OG 이미지 생성. 결과 카드 관련 작업 시 사용.
---

# 결과 카드

## 인앱 결과 화면
첫 스크롤 없이 보이는 영역에 **엠블럼 + 캐릭터명 + 한 줄 정의**만. 나머지는 아래로.
스크린샷 유도 지점을 여기 하나로 고정한다.

블록 순서 (고정):
엠블럼 → 캐릭터명(세리프) → 한 줄 정의 → 스탯 막대 2개 → 한 줄 조합문
→ 유일하게 가진 것 → 조용히 새는 곳 → 다음 한 수 → CTA

한 줄 조합문: `{무기}를 든 {캐릭터}, {길}을 걷고 있습니다.`

## 공유 이미지
- `@vercel/og`로 `/api/og?c={character}&w={weapon}&p={path}` 동적 생성
- 1080×1350. 인스타 스토리 9:16 크롭에서 캐릭터명이 잘리지 않게 상단 안전영역 확보
- 엠블럼 PNG + 무기 배지 PNG를 서버에서 합성. 클라이언트 canvas 사용 금지(모바일 실패율 높음)
- 결과 URL은 `/r/{sessionId}` — 공유 링크를 열면 같은 카드가 보여야 한다

## 금지
숫자 점수, 퍼센타일(실데이터 100건 이전), 이모지, 그라디언트.
```

---

## 6. skills/visual-system/SKILL.md (토큰만)

```markdown
---
name: visual-system
description: 컬러·타이포·간격 토큰. UI를 만들거나 수정할 때 항상 사용.
---

## 컬러
--bg: #F7F5F1;  --ink: #1A1A1A;  --gray: #9B9B9B;
--c-jangin: #4A5568;  --c-teojut: #8B4A3B;  --c-myeongjang: #B8862B;
--c-wonjeong: #1F4E79; --c-sangdan: #3F6F5A; --c-yeongju: #5B3A6E;

## 타이포
캐릭터명만 세리프(본명조/나눔명조 Bold). 그 외 전부 Pretendard.
캐릭터명 72 / 한자 40 / 정의 34 / 블록제목 26(700) / 본문 28(400, 행간 1.65)

## 금지
그라디언트, 드롭섀도, 글로우, 캐릭터 간 채도 차이, 액센트 컬러 2개 이상 동시 사용.
```

---

## 7. workflows/ 슬래시 커맨드

**`/startcycle`** — 새 기능 하나를 처음부터 끝까지
```
1. PM 에이전트: Implementation Plan 작성 → 사용자 승인 대기
2. 백엔드 → 프론트엔드 순으로 서브에이전트 병렬 실행
3. QA 브라우저 서브에이전트: 3개 시나리오 완주 + 390×844 녹화
4. Walkthrough 아티팩트로 결과 보고 (녹화 첨부 필수)
```

**`/verify`** — 배포 전 검수 전용
```
1. 채점 테스트 케이스 4개 실행
2. 12문항 화면 전부 390×844 캡처 → 선택지 접힘 여부 확인
3. 결과 30조합 중 무작위 6개 OG 이미지 fetch → 렌더 확인
4. 실패 항목만 리스트로 보고
```

---

## 8. 빌드 순서 (Task Group 단위)

| # | Task Group | 완료 기준 |
|---|---|---|
| 1 | 문항 12개 + 셔플 + 진행막대 | 완주해서 콘솔에 `answers[12]` 찍힘 |
| 2 | 채점 로직 + 테스트 4케이스 | 4개 전부 통과 |
| 3 | 결과 화면(카피 6종) | 6캐릭터 전부 육안 확인 |
| 4 | 엠블럼·배지 에셋 투입 | 6종 나란히 놓고 세트로 보임 |
| 5 | OG 공유 이미지 | 카톡·인스타 실기기 미리보기 확인 |
| 6 | Supabase 로깅 + 지표 3개 | 대시보드에서 Q1→Q2 진행률 조회됨 |
| 7 | 마이크로 피드백 (1·5·9번) | 정성 문장 노출 |

**1~3까지가 진짜 MVP다.** 4~5 없이도 테스트는 돌릴 수 있다. 에셋 기다리다 빌드가 멈추는 게 가장 흔한 실패다.

---

## 9. 첫 프롬프트 (그대로 붙여넣기)

```
AGENTS.md와 skills/ 전부 읽고 시작해.

Task Group 1만 진행한다. 범위 밖은 만들지 마.

목표: 12문항 진단 플로우.
- content/questions.ts에 있는 문구를 그대로 쓴다. 한 글자도 바꾸지 마.
- 선택지는 화면에서 셔플하되, 저장은 원본 인덱스로 한다.
- 상단은 얇은 진행 막대 + "약 1분". 숫자 1/12는 쓰지 마.
- 질문 아래 24px 안에 첫 선택지가 오게 한다.
- 마지막 문항 완료 시 콘솔에 answers 배열만 출력하고 멈춘다. 결과 화면은 아직 만들지 마.

완료되면 브라우저 서브에이전트로 390×844에서 12문항 전부 녹화해서 보여줘.
```

---

## 10. 안전 설정

- **Secure Mode 켜기.** 에이전트가 터미널에서 임의 명령을 돌린다. Supabase 키가 있는 프로젝트에서는 필수.
- `.env.local`은 `.gitignore`에 있는지 첫 커밋 전에 직접 확인. 에이전트가 실수로 커밋하는 사고가 가장 흔하다.
- 배포는 에이전트에게 맡기지 말고 Vercel 대시보드에서 직접 누른다. 자동 배포는 검수 습관을 없앤다.

---

## 11. 검수 체크리스트

- [ ] `/verify` 4단계 전부 통과
- [ ] 결과 화면 어디에도 총점·등급·순위가 없다
- [ ] 첫 화면에서 스크롤 없이 선택지 3개가 다 보인다
- [ ] 진행 표시에 `12`라는 숫자가 없다
- [ ] 채점 테스트 4케이스 통과
- [ ] `.env.local`이 커밋되지 않았다
- [ ] 공유 링크를 다른 폰에서 열었을 때 같은 카드가 보인다
